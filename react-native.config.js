/*module.exports = {
    project: {
        ios: {},
        android: {},
    },
    assets: ['./src/fonts'],
};*/
module.exports = {
    project: {
        ios: {},
        android: {}, // grouped into "project"
    },
    assets: ["./assets/fonts/"], // stays the same
};